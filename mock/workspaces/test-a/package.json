{
  "name": "mock-test-a",
  "private": true,
  "scripts": {
    "jill:build": "node -e \"setTimeout(() => process.exit(0), 3000)\"",
    "start": "node -e \"setInterval(() => console.log('running !'), 500); setTimeout(() => process.exit(0), 5000)\""
  },
  "dependencies": {
    "mock-test-b": "workspace:*"
  },
  "devDependencies": {
    "mock-test-c": "workspace:*"
  }
}
