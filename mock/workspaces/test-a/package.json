{
  "name": "mock-test-a",
  "scripts": {
    "build": "node -e \"setTimeout(() => process.exit(0), 3000)\" --",
    "watch": "node -e \"setInterval(() => console.log('hello i\\'m a'), 3000)\" --",
    "start": "node -e \"console.log(process.argv); setInterval(() => console.log('running !'), 500); setTimeout(() => process.exit(0), 5000)\" --"
  },
  "dependencies": {
    "mock-test-b": "workspace:*"
  },
  "devDependencies": {
    "mock-test-c": "workspace:*"
  }
}
