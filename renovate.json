{
  "extends": [
    "config:base"
  ],
  "postUpgradeTasks": {
    "commands": ["yarn version decline --deferred"],
    "fileFilters": ["packages/*/package.json"],
    "executionMode": "branch"
  }
}
